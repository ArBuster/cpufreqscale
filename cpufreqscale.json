{
    // 全局设置, 每个分组的默认设置
    "global": {
        // 基准频率Mhz, scaling_max_freq最低值
        "base": 2400,
        // 最低频率Mhz, scaling_min_freq的值, 设置生效之后不会改变
        "min": 1700,
        // scaling_max_freq的最大值, Mhz
        "max": 4500
        // 上调频率上限的cpu使用率, 默认50
        // "up": 62.5,
        // 下调频率上限的cpu使用率, 默认50
        // "down": 37.5
    }
    /* cpu分组设置, 每个分组是一个json对象
    "groups": [
        {
            // 本组cpu的和逻辑核心编号
            // 支持数字和"m-n"的格式, "m-n"表示从编号m到n的所有核心都纳入本组
            "number": [0, 2, 4, "5-10"],
            // 其它设置和global相同, 仅针对本组的核心有效
            "base": 2200,
            "max": 4600
        }
    ]
    */
}